#include<iostream>

using namespace std;

int main()
{   // enum is use to compare a datatype into a number
    enum Meal{ breakfast, lunch, dinner};
    Meal m1 = lunch;
    cout<<m1 <<endl;
    
    cout<<(m1 == 1)<<endl;
    return 0;

return 0;
}